name: Publish Container

on:
  push:
    branches: [main]
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: docker/setup-buildx-action@v4

      - uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/metadata-action@v6
        id: meta
        with:
          images: ghcr.io/kylejschultz/media-mcp
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=semver,pattern={{version}}
            type=sha,prefix=sha-

      - uses: docker/build-push-action@v7
        with:
          context: .
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  publish-beets-flask-remediation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: docker/setup-buildx-action@v4

      - uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/metadata-action@v6
        id: remediation-meta
        with:
          images: ghcr.io/kylejschultz/beets-flask-remediation
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=semver,pattern={{version}}
            type=sha,prefix=sha-

      - name: Test remediation overlay
        uses: docker/build-push-action@v7
        with:
          context: .
          file: Dockerfile.beets-flask-remediation
          platforms: linux/amd64
          target: test
          push: false

      - name: Publish remediation overlay
        uses: docker/build-push-action@v7
        with:
          context: .
          file: Dockerfile.beets-flask-remediation
          platforms: linux/amd64
          target: runtime
          push: true
          tags: ${{ steps.remediation-meta.outputs.tags }}
          labels: ${{ steps.remediation-meta.outputs.labels }}

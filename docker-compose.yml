services:
  media-mcp:
    image: ghcr.io/kylejschultz/media-mcp:latest
    env_file:
      - .env
    networks:
      - media
    stdin_open: true
    restart: unless-stopped

networks:
  media:
    external: true
    name: docker-network
